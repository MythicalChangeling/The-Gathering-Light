class Play extends Phaser.Scene {
    constructor() {
        super('playScene')
    }
}

//code to add sprites
// let crystal = this.physics.add.sprite(0, 0, 'crystal', 0).setOrigin(0)
// let light = this.physics.add.sprite(0, 0, 'light', 0).setOrigin(0)
// let mc = this.physics.add.sprite(0, 0, 'mc', 0).setOrigin(0)
// let over = this.add.sprite(0, 0, 'game-over', 0).setOrigin(0)

//code for playing animations:
// crystal.play('crystal-get')
// light.play('light-get')
// mc.play('mc-flying')
// over.play('game-over')